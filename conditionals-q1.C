#include <iostream>

int main () {
	int max_length = 50;
	int count = 0;
	char input_line;
	int i = 0;
	
	/* original loop
	for (i; i < max_length; ++i)
		if (input_line[i] == '?') ++count;
	
	rewritten loop
	while (i != max_length)
		if (input_line[*i] == '?') ++count;
		++i;*/
		

}

